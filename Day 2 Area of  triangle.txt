#include <stdio.h>
void main(){
    int b,h,area;
    printf("Enter the base:");
    scanf("%d",&b);
    printf("Enter the height:");
    scanf("%d",&h);
    area=0.5*b*h;
    printf("Area:%d\n",area);
}